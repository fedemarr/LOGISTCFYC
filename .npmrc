# Expo/Metro no resuelve bien el node_modules simlinkeado que pnpm usa por
# defecto en monorepos. Se "hoistea" para evitar errores de resolución de
# módulos en apps/mobile (ver docs/DECISIONES.md).
node-linker=hoisted
shamefully-hoist=true

auto-install-peers=true
strict-peer-dependencies=false
